package Atividade.OperadoresLogicos;

public class Execicio2 {
    public static void main(String[] args) {
        //Declarar uma variável boolean dentroDoIntervalo e atribuir (num >= 10 && num <= 50), exibindo o valor.
        double num = 100;
        boolean dentroDoIntervalo = (num >= 10 && num <= 50);
        System.out.println(dentroDoIntervalo);
    }
}
